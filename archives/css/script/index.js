// Apenas um aviso
alert("Olá! Isso é um pop-up simples.");

// Uma pergunta de sim ou não (retorna true ou false)
const confirmacao = confirm("Deseja excluir este item?");

// Uma caixa para o usuário digitar algo (retorna o texto ou null)
const nome = prompt("Qual é o seu nome?");